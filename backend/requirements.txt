# Debate Coach Backend — Python Dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.128.2
uvicorn[standard]==0.40.0
websockets==16.0
python-multipart==0.0.22

# Config
python-dotenv==1.2.1
pydantic-settings>=2.0

# Database
sqlalchemy==2.0.46
asyncpg==0.31.0
alembic==1.18.3

# HTTP client (for OpenAI/Groq API calls)
httpx==0.28.1
openai>=1.0.0

# Auth
pyjwt>=2.8.0
bcrypt>=4.0.0

# Testing
pytest==8.3.5
pytest-asyncio==0.25.0
aiosqlite>=0.20.0

# STT Providers (cloud path)
deepgram-sdk>=3.0

# TTS Providers
edge-tts>=6.1.0
gTTS>=2.5.0
google-genai>=1.0.0

# VAD (Silero — optional but recommended for better EoT accuracy)
# Optional edge/local dependencies (install only when needed):
# faster-whisper>=1.0.0
# torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
